---
description: Add a note to the long-term RAG knowledge base (Qdrant)
---

Add to RAG: $ARGUMENTS

Use `/root/.claude/scripts/rag-add.py` to embed and store.

Tag with metadata: `{ source, date, project, category }`.

Confirm with: "✓ Запомнено в долгосрочната памет (Qdrant)."

If the script/Qdrant isn't running yet, instruct: "Run `claude-mem search '<query>'` or set up RAG via Phase 17 first."
