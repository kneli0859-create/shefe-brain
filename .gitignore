# Secrets — НИКОГА не commit-вай
.env
.env.local
.env.*.local
*.key
*.pem
credentials.json
.supabase
.aws
.config-hash

# Logs
logs/
*.log
npm-debug.log*

# Dependencies
node_modules/
.pnpm-store/

# Build
.next/
dist/
build/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Memory snapshots (могат да са големи)
memory/snapshots/*.gz

# Backups
memory/backups/*.tar.gz

# Cache
.cache/
.next/cache/

# VoltAgent install snapshot (cloned to agents/installed/voltagent/; not our code)
agents/installed/voltagent/

# Dashboard build artefacts (lives under brain/dashboard/)
dashboard/.next/
dashboard/node_modules/

# ──────────────────────────────────────
# SECRETS — v2.1 LIVING EMPIRE (НИКОГА не commit-вай!)
# ──────────────────────────────────────
.env.api-keys
.env.api-keys.*
.env.crypto
.env.crypto.*
.env.*.backup
.env.*.backup-*
.env.*.bak
*.backup-[0-9]*
.gmail-token.json
credentials.json
secrets/
BRAIN_*_SECRETS.md
*.secrets.md
*-secrets.md

# OAuth tokens
.oauth-tokens/
oauth_token.json

# Crypto trading
crypto/.live-enabled
crypto/api-keys/

# L11 (2026-05-20): planning note with embedded secrets — keep on disk, never commit
docs/notes/2026-05-17-brain-v2-planning/brain-v2-final.md
